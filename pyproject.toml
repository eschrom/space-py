[project]
name = "space-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.3.1",
    "tifffile>=2025.6.11",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
